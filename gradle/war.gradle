apply plugin: "war"

bootWar {
    mainClass = "com.agile2001.tdd.TddDojoApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
